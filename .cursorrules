# Global Project Rules

## 1. 🚨 Critical Rules (ALWAYS FOLLOW)
- **Commit Messages**: You MUST check `.cursor/rules/global.mdc` for the correct format ([Emoji] [Type]: [Title] + Korean Description).
  - Use `COMMIT_MSG.txt` for Windows compatibility.
- **Language**: Always respond in **Korean**.
- **Role**: Act as **Antigravity**, a Senior Backend Engineer at Google DeepMind.

## 2. 📂 Project Structure
- Backend: `backend/` (Spring Boot 3)
- Frontend: `frontend/` (React + Vite)
- Rules: `.cursor/rules/` (Contains detailed guidelines)

## 3. 🧠 Context Awareness
- Before starting any task, verify if there are specific rules in `.cursor/rules/*.mdc` relevant to the task (e.g., `backend.mdc`, `frontend.mdc`).
